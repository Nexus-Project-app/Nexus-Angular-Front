name: CI CD frontend

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'

# SonarQube runs in automatic continuous analysis mode — not invoked here

jobs:
  # ────────────────────────────────────────────────────────────────────────────
  # Quality gates: lint, test, build validation, filesystem vulnerability scan
  # Runs on GitHub-hosted runner for clean, reproducible environment
  # ────────────────────────────────────────────────────────────────────────────
  quality:
    name: Quality Gates
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci 2>&1 | tee npm-install.log

      - name: Lint
        run: npm run lint 2>&1 | tee lint.log

      # - name: Run tests
      #   # ChromeHeadless requires --no-sandbox on CI; configure in karma.config.js
      #   # if ChromeHeadlessNoSandbox launcher is not defined, add:
      #   #   browsers: ['ChromeHeadless']  with  flags: ['--no-sandbox']
      #   run: npm run test -- --watch=false --no-progress --browsers=ChromeHeadless 2>&1 | tee test.log

      - name: Build application
        run: npm run build 2>&1 | tee build.log

      - name: Scan filesystem for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .
          exit-code: '1'
          severity: CRITICAL,HIGH
          format: sarif
          output: trivy-fs-results.sarif

      - name: Upload Trivy filesystem scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: trivy-fs-results.sarif

      - name: Upload quality logs and scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: quality-logs-${{ github.sha }}
          path: |
            npm-install.log
            lint.log
            test.log
            build.log
            trivy-fs-results.sarif
          retention-days: 30

  # ────────────────────────────────────────────────────────────────────────────
  # Docker: build production image, push to GHCR, scan image, attest provenance
  # ────────────────────────────────────────────────────────────────────────────
  docker:
    name: Build & Push Docker Image
    runs-on: [self-hosted, nexus, front]
    needs: quality
    permissions:
      contents: read
      packages: write
      id-token: write      # required for provenance attestation
      security-events: write  # required for SARIF upload

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to dhi.io registry
        uses: docker/login-action@v3
        with:
          registry: dhi.io
          username: ${{ secrets.DHI_USERNAME }}
          password: ${{ secrets.DHI_PASSWORD }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch,suffix=-latest
            type=sha,prefix=sha-,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          provenance: true
          sbom: true
          build-args: |
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
            GIT_REVISION=${{ github.sha }}
            VERSION=${{ github.ref_name }}

      - name: Scan Docker image for vulnerabilities (Trivy)
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ghcr.io/${{ github.repository }}@${{ steps.build.outputs.digest }}
          exit-code: '1'
          severity: CRITICAL,HIGH
          format: sarif
          output: trivy-image-results.sarif

      - name: Upload Trivy scan results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@4dd16135b986a4d4109ea826f34f1375bcc19b94  # v3.25.15
        if: always()
        with:
          sarif_file: trivy-image-results.sarif

      - name: Upload Trivy SARIF artifact
        if: always()
        uses: actions/upload-artifact@65d862660abb392b8c4737dddae405b503b8d493  # v4.3.6
        with:
          name: trivy-image-results-${{ github.sha }}
          path: trivy-image-results.sarif
          retention-days: 30

  # ────────────────────────────────────────────────────────────────────────────
  # Deploy: pull latest image and redeploy via docker compose
  # Runs only on push to main (not on PRs)
  # ────────────────────────────────────────────────────────────────────────────
  deploy:
    name: Deploy Nexus Frontend
    runs-on: [self-hosted, nexus, front]
    needs: docker
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    env:
      COMPOSE_DIR: /opt/nexus

    steps:
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@a2c9ff5db28f39f8e93d8b47c5e89e0e47e09e3b  # v3.3.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull & redeploy
        working-directory: ${{ env.COMPOSE_DIR }}
        run: |
          docker compose pull
          docker compose up -d --remove-orphans
